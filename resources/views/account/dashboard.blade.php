@extends('layouts.account')
@section('title','Akun Saya — Kanrejawataa')
@section('account-content')
<div class="space-y-6"><section class="rounded-3xl bg-stone-950 p-7 text-white"><p class="text-sm font-bold text-amber-400">Selamat datang kembali</p><h1 class="mt-2 text-3xl font-black">{{ auth()->user()->name }}</h1><p class="mt-2 text-stone-300">Kelola profil, keranjang, pembayaran, dan status pesananmu.</p></section><section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach(['diproses'=>'Diproses','siap_diambil'=>'Siap diambil','dikirim'=>'Dikirim','selesai'=>'Selesai'] as $key=>$label)<div class="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><p class="text-sm text-stone-500">{{ $label }}</p><p class="mt-2 text-3xl font-black text-amber-700">{{ $orderSummary[$key] ?? 0 }}</p></div>@endforeach</section><section class="rounded-3xl border border-stone-200 bg-white p-6 shadow-sm"><div class="flex justify-between"><h2 class="text-xl font-black">Pesanan terbaru</h2><a href="{{ route('orders.index') }}" class="text-sm font-bold text-amber-700">Lihat semua →</a></div><div class="mt-4 divide-y divide-stone-100">@forelse($latestOrders as $order)<a href="{{ route('orders.show',$order) }}" class="flex flex-wrap items-center justify-between gap-3 py-4"><div><b>{{ $order->order_number }}</b><p class="text-sm text-stone-500">{{ $order->ordered_at->format('d M Y') }}</p></div><div class="flex items-center gap-2"><x-status-badge :status="$order->status"/><b>Rp {{ number_format($order->total,0,',','.') }}</b></div></a>@empty<p class="py-8 text-center text-stone-500">Belum ada pesanan.</p>@endforelse</div></section></div>
@endsection
