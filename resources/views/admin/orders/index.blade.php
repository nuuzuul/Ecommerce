@extends('layouts.admin')
@section('title','Pesanan — Admin Kanrejawataa')
@section('page-title','Kelola Pesanan')
@section('content')
<div><h2 class="text-2xl font-black">Daftar pesanan</h2><p class="text-sm text-stone-500">Verifikasi pembayaran dan perbarui proses pengantaran.</p></div>
<form class="mt-5 grid gap-3 rounded-2xl border border-stone-200 bg-white p-4 md:grid-cols-[1fr_200px_220px_auto]"><input name="search" value="{{ request('search') }}" class="form-input" placeholder="Nomor order atau nama pembeli"><select name="status" class="form-input"><option value="">Semua status pesanan</option>@foreach(['diproses','siap_diambil','dikirim','selesai'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucwords(str_replace('_',' ',$status)) }}</option>@endforeach</select><select name="payment_status" class="form-input"><option value="">Semua pembayaran</option>@foreach(['belum_bayar','menunggu_verifikasi','sudah_bayar'] as $status)<option value="{{ $status }}" @selected(request('payment_status')===$status)>{{ ucwords(str_replace('_',' ',$status)) }}</option>@endforeach</select><button class="btn-primary">Filter</button></form>
<div class="mt-6 overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="admin-table"><thead><tr><th>Order</th><th>Pembeli</th><th>Pengantaran</th><th>Total</th><th>Pembayaran</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><b>{{ $order->order_number }}</b><p class="text-xs text-stone-500">{{ $order->ordered_at->format('d M Y H:i') }}</p></td><td>{{ $order->user->name }}</td><td>{{ $order->delivery_label }}</td><td><b>Rp {{ number_format($order->total,0,',','.') }}</b></td><td><x-status-badge :status="$order->payment_status"/></td><td><x-status-badge :status="$order->status"/></td><td><a href="{{ route('admin.orders.show',$order) }}" class="table-action">Detail</a></td></tr>@empty<tr><td colspan="7" class="text-center text-stone-500">Belum ada pesanan.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $orders->links() }}</div>
@endsection
