@extends('layouts.admin')
@section('title',$product->name.' — Admin')
@section('page-title','Detail Produk')
@section('content')
<div class="grid gap-6 lg:grid-cols-[360px_1fr]"><div class="overflow-hidden rounded-3xl border border-stone-200 bg-white p-3 shadow-sm"><img src="{{ $product->image_url }}" class="aspect-[4/3] w-full rounded-2xl object-cover"></div><div class="rounded-3xl border border-stone-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap justify-between gap-3"><div><span class="rounded-full bg-amber-100 px-3 py-1 text-xs font-bold text-amber-800">{{ $product->category->name }}</span><h1 class="mt-3 text-3xl font-black">{{ $product->name }}</h1></div><div class="flex gap-2"><a href="{{ route('admin.products.edit',$product) }}" class="btn-primary">Edit</a><a href="{{ route('products.show',$product) }}" class="btn-secondary">Lihat publik</a></div></div><p class="mt-5 leading-7 text-stone-600">{{ $product->description }}</p><div class="mt-6 grid gap-3 sm:grid-cols-2">@foreach($product->variants as $variant)<div class="rounded-2xl bg-amber-50 p-4"><p class="text-sm font-bold text-stone-500">{{ $variant->label ?: 'Tanpa ukuran' }}</p><p class="mt-1 text-xl font-black text-amber-800">Rp {{ number_format($variant->price,0,',','.') }}</p><p class="mt-1 text-sm">Stok: <b>{{ $variant->stock }}</b></p></div>@endforeach</div><div class="mt-6 flex flex-wrap gap-3 text-sm"><span class="rounded-full bg-stone-100 px-3 py-1 font-bold">{{ $product->is_active ? 'Aktif':'Nonaktif' }}</span>@if($product->is_featured)<span class="rounded-full bg-amber-100 px-3 py-1 font-bold text-amber-800">Produk unggulan</span>@endif</div></div></div>
@endsection
